# Allreva Harness 작업 안내

이 저장소는 Agent가 실행하는 Skill과 역할 정의만 관리한다. 사람용 정책, 아키텍처, RFC, ADR은 `Allreva_Docs`를 기준으로 한다.

## 원칙

1. 자동으로 긴 workflow를 시작하지 않는다. 작업 성격에 맞는 Skill과 역할만 명시적으로 사용한다.
2. main session은 사용자와의 결정, 작업 계획, 최종 통합을 맡는다.
3. `scout`과 `reviewer`는 읽기 전용이다. 실제 수정은 하나의 writer만 한다.
4. 역할 정의는 짧은 입력과 구조화된 출력을 우선한다. 전체 대화 기록이나 불필요한 파일 목록을 넘기지 않는다.
5. 실행 규칙이 바뀌면 이 저장소를, 사람용 규칙이 바뀌면 `Allreva_Docs`를 수정한다.
